import { Server } from "socket.io";
import { createServer } from "http";
import { io as Client } from "socket.io-client";

// Not actually testing the backend server, I want to see if the main server prints an error when adding a user via socket
